# Regression fixture routing

The immutable v2.0 defect deck and its non-sensitive QA evidence are stored at:

`regression/fixtures/synthetic_cardio_aki_v2_0/`

The test suite references that directory rather than duplicating the binary
PPTX or any scientific inputs. No patient-level data or answer files are stored
under this test directory.
